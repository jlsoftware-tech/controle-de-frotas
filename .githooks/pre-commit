#!/usr/bin/env bash
set -e

mapfile -d '' STAGED_FILES < <(git diff --cached --name-only --diff-filter=ACMR -z -- '*.php')

if [ ${#STAGED_FILES[@]} -eq 0 ]; then
    exit 0
fi

vendor/bin/pint -- "${STAGED_FILES[@]}"

git add -- "${STAGED_FILES[@]}"
